import UIKit

struct Constant {
    struct Size {
        static let DeviceWidth = UIScreen.mainScreen().bounds.size.width
        static let DeviceHeight = UIScreen.mainScreen().bounds.size.height
    }
}

class LayoutManager: NSObject {
   
}
